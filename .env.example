# Security Credentials (DO NOT commit the actual .env file with real values to version control)
# Copy this file to .env and replace the example values with your actual secrets

# JWT settings
JWT_SECRET=replace_with_a_very_long_and_secure_random_secret_key

# Client API Keys
USER_CLIENT_API_KEY=replace_with_secure_user_client_key
ADMIN_CLIENT_API_KEY=replace_with_secure_admin_client_key
